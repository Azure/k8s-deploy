import { Artifact, TargetResource, WorkflowRun } from "./models";
export declare class DeploymentReport {
    artifacts: Artifact[];
    workflowRun: WorkflowRun;
    targetResource: TargetResource;
    private _githubClient;
    initialize(gitHubToken: string, artifacts?: Artifact[], pipelineStatus?: string, targetResource?: TargetResource): Promise<void>;
    addArtifacts(artifacts: Artifact[]): void;
    setTargetResource(targetResource: TargetResource): void;
    setPipeline(status: string): Promise<void>;
    export(): string;
    private _getWorkflowFilePath;
}
