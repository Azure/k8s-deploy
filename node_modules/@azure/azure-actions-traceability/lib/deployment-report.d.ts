import { Artifact, TargetResource, Pipeline } from "./models";
export declare class DeploymentReport {
    artifacts: Artifact[];
    pipeline: Pipeline;
    targetResource: TargetResource;
    constructor(artifacts?: Artifact[], pipelineStatus?: string, targetResource?: TargetResource);
    addArtifacts(artifacts: Artifact[]): void;
    setTargetResource(targetResource: TargetResource): void;
    setPipeline(status: string): void;
    export(): string;
    private getGitHubPipelineProperties;
}
